# Server
PORT=3000
NODE_ENV=development

# Authentication
JWT_SECRET=your-secret-key-here-change-in-production
VITE_APP_ID=meta-ads-dashboard

# OAuth (optional - leave empty to disable authentication)
OAUTH_SERVER_URL=
VITE_OAUTH_PORTAL_URL=
OWNER_OPEN_ID=

# Database (optional - leave empty to run without database)
# Uncomment and configure if you want to use database features:
# DATABASE_URL=mysql://user:password@host:port/database_name

# AI Assistant Configuration (REQUIRED for AI features)
# ⚠️ CRITICAL: This API key is used ONLY by the AI Assistant
# The AI Assistant DOES NOT have access to Meta Ads API
# It only analyzes preprocessed data sent from the frontend
#
# Options:
# 1. OpenAI API: Get key from https://platform.openai.com/api-keys
#    Set BUILT_IN_FORGE_API_URL to https://api.openai.com (or leave empty for default)
#    Set BUILT_IN_FORGE_API_KEY to your OpenAI key (sk-...)
#
# 2. Claude API: Get key from https://console.anthropic.com/
#    Set BUILT_IN_FORGE_API_URL to https://api.anthropic.com
#    Set BUILT_IN_FORGE_API_KEY to your Claude key
#
# 3. Other OpenAI-compatible APIs (like Forge, Azure OpenAI, etc.)
#    Set BUILT_IN_FORGE_API_URL to your API endpoint
#    Set BUILT_IN_FORGE_API_KEY to your API key
#
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
VITE_FRONTEND_FORGE_API_KEY=
